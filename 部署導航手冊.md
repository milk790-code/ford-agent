# 福特雲端代理人 — 部署導航手冊（給陳學誼）

> 你不用寫程式。整包程式我寫好了，你照下面「去哪、點什麼、貼哪裡」做一次就上線。
> 全程約 10–15 分鐘。**有 ⚠ 的步驟會動到線上服務或計費，放慢看清楚再做。**
> 憑證一律你自己親手輸入；我不會、也不該替你輸入。

---

## 這包東西是什麼

一個跑在你自己 Cloudflare 帳號上的小程式（Worker），做三件事：
1. **每週一早上自動**去抓三條乾淨公開來源（政府總市場領牌數、PTT 福特菜單、U-CAR 福特新聞），整理成一份資料。
2. 開一個網址 `你的網址/data`，讓那支送主管的工具自動讀到最新資料。
3. 開一個網址 `你的網址/vision`，主管在工具裡拍政策表照片，這裡用 Cloudflare 自己的 AI 辨識文字回傳。

**核心完全不需要 Anthropic 金鑰**，全走你 Cloudflare 帳號，費用幾乎是零（你的用量遠在免費額度內）。

---

## A. 準備（一次性）

1. 有一個 **GitHub 帳號**、一個 **Cloudflare 帳號**（你都有）。
2. 把這個資料夾（ford-agent）整包上傳成一個 GitHub repo：
   - 去 https://github.com/new ，Repository name 填 `ford-agent`，建立。
   - 把這包檔案全部拖進去上傳（或用你慣用的方式 push）。

## B. 部署 Worker（用網頁，最省事）⚠ 這步會把服務開上線

1. 去 https://dash.cloudflare.com → 左側 **Workers & Pages** → **Create** → **Create Worker**。
2. 名稱填 `ford-agent` → **Deploy**（先讓它建起來，等等用我們的程式覆蓋）。
3. 進到這個 Worker → **Settings** 分頁，先做三個綁定：
   - **Variables and Secrets** 區，這版預設**不用填任何金鑰**，跳過。
   - **Bindings** → 新增 **R2 bucket**：⚠ 先去左側 **R2** 建一個 bucket 叫 `ford-agent-data`（Create bucket，名字要一模一樣）。回來這裡 binding 名稱填 **`FORD_R2`**、bucket 選 `ford-agent-data`。
   - **Bindings** → 新增 **Workers AI**：binding 名稱填 **`AI`**。
4. 回到 repo，用 GitHub 連動部署（最穩）：
   - Cloudflare 該 Worker → **Settings → Build** → 連接你的 `ford-agent` GitHub repo。
   - 或者：在 repo 的 **Settings → Secrets and variables → Actions** 加兩個 secret（⚠ 貼進 GitHub 的 Secret 區，不是程式裡）：
     - `CLOUDFLARE_API_TOKEN`：去 Cloudflare **My Profile → API Tokens → Create Token**，⚠ 範圍只給 **Workers Scripts: Edit**（最小權限）。
     - `CLOUDFLARE_ACCOUNT_ID`：在 Cloudflare 首頁右側可複製。
   - 加完後，repo 每次 push 到 main 就會自動部署（deploy.yml 已寫好）。

## C. 開通視覺模型（一次性）⚠ 需同意 Meta 授權

第一次用 Llama 3.2 Vision 要先同意一次授權，照做一次即可：
- 用瀏覽器打開（或在終端機跑）一次這個（把 ACCOUNT_ID、你的 Cloudflare API Token 換上）：
  `curl https://api.cloudflare.com/client/v4/accounts/【你的ACCOUNT_ID】/ai/run/@cf/meta/llama-3.2-11b-vision-instruct -X POST -H "Authorization: Bearer 【你的Token】" -d '{"prompt":"agree"}'`
- 看到回應就代表開通了。（這把 Token 是 Cloudflare 的、不是 Anthropic 的。）

## D. 第一次灌資料 + 測試

1. 瀏覽器打開 `https://ford-agent.【你的子域】.workers.dev/refresh` → 看到一串 JSON、`"ok":true` 就成功抓到資料了。⚠ 這會真的去抓那三個來源一次。
2. 再打開 `.../data` → 應該看到剛抓的資料。
3. 打開 `.../health` → 看到 `ok:true` 代表服務活著。

## E. 把工具接上代理人

1. 打開那支 **新車部銷售政策工作台_連網版.html**。
2. 進「**福特情報→戰略**」分頁 → 在「☁ 雲端代理人」那格貼上你的 Worker 網址（`https://ford-agent.xxx.workers.dev`）→ 按**儲存**。
3. 它會自動讀到資料、顯示 PTT 最新菜單與福特新聞。
4. 右下角**橘色相機鈕**：拍一張政策表或菜單 → 自動送辨識 → 文字跳出來，核對後複製貼到欄位。
   - ⚠ 數字一定要跟原始政策表核對，辨識只是幫你快速帶入、不保證精確；大表字密就分區拍、拍清楚。

> 送主管的版本：你可以先在自己手機把網址設定好再轉存，或直接把網址告訴主管讓他貼一次。工具檔案本身**不含任何金鑰**，可以安心傳。

---

## 選用：想要更準的辨識（之後再說，非必要）

預設的 Cloudflare AI 對乾淨菜單、單價數字夠用。若哪天要辨識「整張密密麻麻的中文政策大表」想更準：
- 在 Cloudflare 該 Worker → **Settings → Variables and Secrets**：
  - 加一般變數 `VISION_PROVIDER` = `anthropic`
  - ⚠ 加 **Secret**（不是一般變數）`ANTHROPIC_API_KEY` = 你自己的新金鑰（**親手貼進這裡，絕不寫進程式、絕不貼進對話**）
- 存檔即生效，程式會自動改走前沿視覺模型。不想用就把這兩個刪掉，回到免金鑰模式。

---

## 安全備忘（重要）
- ⚠ 你之前貼出來那把 Anthropic 金鑰已外洩，**務必已撤銷**。本系統預設根本不用它。
- 所有金鑰只存在 Cloudflare/GitHub 的 Secret 區，**永遠不寫進程式碼、不放進送人的工具檔**。
- Cloudflare API Token 只給最小權限（Workers Scripts: Edit）。
- 這套只抓公開來源、每週一次，不碰任何反爬系統。

## 自主判斷區塊（事後檢視用）
- 視覺辨識預設選 **Cloudflare Workers AI / Llama 3.2 11B Vision**：理由＝同一朵雲、零外部金鑰、工具可安心送人；保留 `VISION_PROVIDER` 開關供日後升級前沿模型。
- 爬蟲架構依沙盒實測結果定為「**PTT 多關鍵字主幹 + 政府CSV錨點 + U-CAR機會性新聞層**」：理由＝實測 U-CAR 無福特專屬列表頁、首頁福特新聞時有時無，故不當主幹；PTT 搜尋實測穩定回傳福特菜單。
- Cron 設 `0 1 * * MON`（台灣週一09:00）。
- 未沙盒驗證項：`/vision` 端點需在 Cloudflare 上實跑（沙盒無 AI 綁定）；Workers AI 首用需 `agree` 授權（已寫入步驟 C）。
